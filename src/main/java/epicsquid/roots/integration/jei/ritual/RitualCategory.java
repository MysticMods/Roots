package epicsquid.roots.integration.jei.ritual;

public class RitualCategory { //implements IRecipeCategory<RitualWrapper> {
/*  private final IDrawable background;

  public RitualCategory(IGuiHelper helper) {
    this.background = helper.createDrawable(new ResourceLocation(Roots.MODID, "textures/gui/jei/ritual_crafting.png"), 0, 0, 140, 76);
  }

  @Override
  public String getUid() {
    return JEIRootsPlugin.RITUAL;
  }

  @Override
  public String getTitle() {
    return I18n.format("container." + JEIRootsPlugin.RITUAL + ".name");
  }

  @Override
  public String getModName() {
    return Roots.NAME;
  }

  @Override
  public IDrawable getBackground() {
    return background;
  }

  @Override
  public void setRecipe(IRecipeLayout recipeLayout, RitualWrapper recipeWrapper, IIngredients ingredients) {
    IGuiItemStackGroup group = recipeLayout.getItemStacks();
    RitualBase recipe = recipeWrapper.recipe;
    List<List<ItemStack>> data = ingredients.getInputs(VanillaTypes.ITEM);
    group.init(0, true, 26, 2);
    group.set(0, data.get(0));
    group.init(1, true, 0, 23);
    group.set(1, data.get(1));
    group.init(2, true, 52, 23);
    group.set(2, data.get(2));
    group.init(3, true, 7, 56);
    group.set(3, data.get(3));
    group.init(4, true, 47, 56);
    group.set(4, data.get(4));
    group.init(5, false, 99, 23);
    group.set(5, new ItemStack(recipe.getIcon()));
  }*/
}
